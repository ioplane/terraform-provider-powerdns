"""Repository automation, importable so the checks can share code and be tested.

Nothing here is published. The package exists because `python -m` needs one,
and because a check with no importable surface can only be tested by running
it, which is how the shell versions came to have no tests at all.
"""
