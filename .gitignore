bin/
dist/
vendor/
.cache/
.worktrees/
coverage.out
coverage.html
__pycache__/
*.pyc
*.tfstate
*.tfstate.backup
.terraform/

# uv creates this in the working tree when a task runs `uv run`.
.venv/

# Task's local checksum cache for `sources`/`generates`.
.task/
