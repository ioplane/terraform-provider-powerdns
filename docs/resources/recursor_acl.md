---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "powerdns_recursor_acl Resource - powerdns"
subcategory: ""
description: |-
  One of the Recursor's two writable netmask settings.
  ws-recursor.cc registers allow-from and allow-notify-from as separate handlers rather than one parameterised route, so every other name answers 404 on read as well as write. This resource rejects any other name before sending.
  Needs webservice.api_dir. Deleting the resource leaves the setting as it is: clearing an ACL that decides who may query is not a safe default.
---

# powerdns_recursor_acl (Resource)

One of the Recursor's two writable netmask settings.

`ws-recursor.cc` registers `allow-from` and `allow-notify-from` as separate handlers rather than one parameterised route, so every other name answers 404 on read as well as write. This resource rejects any other name before sending.

Needs `webservice.api_dir`. Deleting the resource leaves the setting as it is: clearing an ACL that decides who may query is not a safe default.

## Example Usage

```terraform
# One of the Recursor's two writable settings. ws-recursor.cc registers
# allow-from and allow-notify-from as separate handlers rather than one
# parameterised route, so every other name answers 404 on read as well as
# write — and this resource rejects any other name before sending.
#
# Destroying it leaves the setting as it is and warns. There is no unset state
# for an ACL, and writing an empty list would refuse every client.

resource "powerdns_recursor_acl" "clients" {
  setting  = "allow-from"
  netmasks = ["10.0.0.0/8", "192.168.0.0/16", "2001:db8::/32"]
}

resource "powerdns_recursor_acl" "notifies" {
  setting  = "allow-notify-from"
  netmasks = ["192.0.2.53/32"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `netmasks` (List of String) The netmasks, compared as subnets and ignoring order.
- `setting` (String) `allow-from` or `allow-notify-from`.

### Read-Only

- `id` (String) The setting name.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import powerdns_recursor_acl.clients allow-from
```
