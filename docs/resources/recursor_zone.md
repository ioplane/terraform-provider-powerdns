---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "powerdns_recursor_zone Resource - powerdns"
subcategory: ""
description: |-
  A zone on the PowerDNS Recursor.
  Shares a name with powerdns_zone and nothing else: a Recursor zone is either a forward instruction or a small authoritative island, with no DNSSEC, metadata or SOA.
  Every write needs webservice.api_dir. Without it the Recursor is read-only and this provider reports the setting rather than the status code.
---

# powerdns_recursor_zone (Resource)

A zone on the PowerDNS Recursor.

Shares a name with `powerdns_zone` and nothing else: a Recursor zone is either a forward instruction or a small authoritative island, with no DNSSEC, metadata or SOA.

**Every write needs `webservice.api_dir`.** Without it the Recursor is read-only and this provider reports the setting rather than the status code.

## Example Usage

```terraform
# Every Recursor write needs webservice.api_dir. Without it the Recursor is
# read-only and this provider reports the setting rather than the status code.

resource "powerdns_recursor_zone" "internal" {
  name    = "corp.example."
  kind    = "Forwarded"
  servers = ["192.0.2.53", "192.0.2.54"]

  # Set when forwarding to a resolver, cleared when forwarding to an
  # authoritative server. Getting it wrong produces answers that look like a
  # broken upstream.
  recursion_desired = true
}

# The Recursor appends :53 to an address given without a port, so
# "192.0.2.53" and "192.0.2.53:53" are the same upstream and do not produce a
# diff. A different port is a real change.
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `kind` (String) `Forwarded` to ask `servers`, or `Native` to answer from records held locally.
- `name` (String) The zone name.

### Optional

- `notify_allowed` (Boolean) Accept NOTIFY for this zone.
- `recursion_desired` (Boolean) Set the RD bit on forwarded queries. Wanted when forwarding to a resolver, not when forwarding to an authoritative server — getting it wrong produces answers that look like a broken upstream.
- `servers` (List of String) Upstreams for a `Forwarded` zone, as `host` or `host:port`.

The Recursor appends `:53` to an address given without one, so `192.0.2.53` and `192.0.2.53:53` are the same upstream and do not produce a diff. A different port is a real change.

### Read-Only

- `id` (String) The canonical zone name.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
terraform import powerdns_recursor_zone.internal corp.example.
```
